# Kits Still Not Matched To ANH Donors

Checkpoint date: 2026-04-27

Source-of-truth rule: `ANH donors.xlsx` sheet `SWANH donors` is treated as the canonical ANH donor list. This report lists remaining database kits that do not match that sheet by exact Scalemates URL or normalized brand/name.

Before this checkpoint, `ITT Vintage microchip`, `Plastruct Lamp shade`, and `Smartlouvre Koolshade` were removed as non-kit/component records. That reduced the unmatched list from 28 to 25 kits.

The old combined Aurora record `38` (`U.S. Army M8E2 Munitions Carrier and 8" Howitzer`) was split into the two canonical ANH donor rows: `263` (`U.S. Army 8" Howitzer`) received `npn_056`, and `264` (`U.S. Army M8E2 Munitions Carrier`) received the remaining six parts plus the Coffman `41` reference.

The old Aurora record `39` (`U.S.A. M4A3E8 Sherman`) was merged into canonical ANH donor kit `265` (`World War II Sherman Tank`).

The old Aurora record `40` (`WORLD WAR II CHURCHILL TANK`) was merged into canonical ANH donor kit `262` (`British Churchill Tank`).

The old Bandai record `61` (`Tank Destroyer (Sd.Kfz173) Jagd-Panther`) was merged into canonical ANH donor kit `276` (`Tank Destroyer Jagd-Panther`).

The old Frog record `74` (`Blackburn Shark Torpedo-bomber`) was merged into canonical ANH donor kit `284` (`Blackburn Shark Torpedo-bomber`).

The old Fujimi record `77` (`Water Line Series U.S.A. Battleship Missouri`) was merged into canonical ANH donor kit `289` (`U.S.A. Battleship Iowa / Missouri`).

The unused Fujimi record `80` (`Panther-G German Medium Tank`) was merged into canonical ANH donor kit `330` (`Nitto Panther-G German Medium Tank`).

The old Fujimi record `81` (`German Medium Tank Panzer.IV Ausf.J`) was merged into canonical ANH donor kit `328` (`Nitto German Medium Tank Panzer.IV Ausf.J`).

The old Fujimi record `82` (`U.S. Army Tank Destroyer M36 Jackson Tank with Harley Davidson motorcycle`) was merged into canonical ANH donor kit `334` (`Nitto / RiKo U.S. Army Tank Destroyer M36 Jackson Tank with Harley Davidson motorcycle`).

The old Fujimi/Nitto record `84` (`M3A1 Half Track`) was merged into canonical ANH donor kit `329` (`Nitto M3A1 Half Track`).

The Hasegawa record `87` (`Mörser Karl German 600mm Mörser "Karl" On Railway Carrier`) was removed as user-confirmed unused for active ANH donor truth.

The Hasegawa record `88` (`Panzerkampfwagen VI Tiger`) was removed as user-confirmed not a donor. The unmatched list is now 13 kits.

The old Hasegawa record `90` (`G.M.C. CCKW-353 Cargo Truck`) was merged into canonical ANH donor kit `294` (`G.M.C. CCKW-353 Gasoline Tank Truck`). The unmatched list is now 12 kits.

The old Nichimo record `103` (`Tiger II (King TIger)`) was merged into canonical ANH donor kit `321` (`Pz.Kpfw.VI Tiger II Henschel Turret (King Tiger)`). The unmatched list is now 11 kits.

The old Nichimo record `106` (`132 Sherman US Army Medium Tank M4A1`) was merged into canonical ANH donor kit `320` (`M4A1 Sherman`). The unmatched list is now 10 kits.

The remaining confirmed batch was applied: `107` -> `326` (`Hyuga` into `Ise / Hyuga`), `108` -> `325` (`Yamato` source confirmed as `Musashi`), `129` -> `354` (`Lotus 49` into `Lotus 49B`), `134` -> `359` (`Jagdpanther` into `Panther A / Jagdpanther`), `148` -> `362` (`Leopard`), `162` -> `368` (`Avro Lancaster`), `163` -> `377` (`Enterprise` into `Hornet / Enterprise`), `165` -> `376` (`Kumano` into `Suzuya / Mogami / Kumano`), and `167` -> `363` (`M60A1` into `M60A1 / M60A2`).

The Tamiya record `135` (`Royal Army Chieftain Battle Tank`) was removed as user-confirmed removal from active ANH donor truth. The unmatched list is now 0 kits.

The AMT `Fruehauf 40ft. Reefer "Watkins"` record `222` was corrected to its own Scalemates URL: `https://www.scalemates.com/de/kits/amt-t507-40ft-reefer--178230`. This resolved the remaining duplicate URL warning against the AMT tanker.

The Tamiya `M3 Lee` record `137` was merged into canonical donor kit `378` (`Tamiya-MRC M41 Walker Bulldog with 4 Army figures`) after user confirmation that the evidence belongs to the M41 figures/backpack source. The duplicate direct Y-Wing placement already present on the target was collapsed instead of duplicated.

| ID | Brand | Scale | Kit | Serial | Direct placements | Part placements | Parts | Regions | Note |
|---:|---|---|---|---|---:|---:|---:|---:|---|
No unmatched kits remain.

Remaining global cleanup notes:

- No duplicate Scalemates URL groups remain.
- No `Discarded` sheet hits remain active.
